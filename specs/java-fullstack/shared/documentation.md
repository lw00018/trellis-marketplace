# 文档规范

## 文档语言

所有 spec 文档必须使用中文编写。

## 编写原则

- 文档必须描述可执行规则，而不是泛泛建议。
- 强制规则要能在代码审查或 CI 中验证。
- 示例必须贴近 Spring Boot、REST API、MyBatis-Plus 和 Maven。
- 文档变更必须同步索引和 README。

## 维护规则

- 新增文档后更新对应目录的 `index.md`。
- 新增重大问题后更新 `big-question/index.md`。
- 修改 API、数据库、事务、安全规范时同步相关指南。
- 过时规则必须删除或标注替代方案。
