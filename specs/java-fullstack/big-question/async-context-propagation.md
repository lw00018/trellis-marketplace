# 异步上下文传递

## 现象

- 异步日志没有 traceId。
- 异步任务中获取不到当前用户或租户。
- 链路追踪在异步边界断开。

## 根因

MDC、ThreadLocal 和安全上下文默认绑定当前线程，切换线程后不会自动携带。

## 解决方案

- 使用任务装饰器显式复制 MDC。
- 异步任务参数中显式传递用户 ID、租户 ID 和业务 ID。
- 不依赖隐式 ThreadLocal 传递关键业务数据。

## 预防

- 线程池统一封装。
- 异步失败必须有日志和告警。
- 测试覆盖异步任务中的上下文字段。
