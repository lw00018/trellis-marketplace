# Bean 循环依赖

## 现象

- 应用启动失败，提示 Bean 之间存在循环依赖。
- 通过懒加载暂时解决后运行时仍出现职责混乱。

## 根因

两个或多个 Service 互相依赖，通常说明领域边界不清或职责放错位置。

## 解决方案

- 抽取共同依赖到独立服务。
- 使用领域事件降低直接依赖。
- 通过接口重新定义依赖方向。
- 合并本就属于同一职责的服务。

## 禁止

- 禁止开启全局循环依赖配置作为长期方案。
- 禁止用字段注入掩盖循环依赖。
