# N+1 查询问题

## 现象

- 查询一个列表后，每条记录又触发一次详情查询。
- 数据量小时正常，数据量增长后接口延迟急剧上升。

## 根因

代码按对象逐条查询关联数据，导致 1 次主查询 + N 次子查询。

## 解决方案

- 使用批量查询，把 ID 列表一次性查出。
- 使用 JOIN 或专门的聚合查询 DTO。
- 对外部接口批量查询控制并发和批大小。
- 在测试和日志中关注 SQL 次数。

## 预防

- 列表接口必须评估数据量和关联查询。
- 禁止在循环中执行 Mapper 查询。
- 慢 SQL 和 SQL 次数纳入性能测试。
