# CSS 与布局规范

## 布局

- 页面布局优先使用 Flex 和 Grid。
- 容器、间距、断点必须使用设计系统变量。
- 不用魔法像素修复布局，先分析父子尺寸和盒模型。
- 移动端和桌面端必须明确响应式策略。

## 样式组织

- 通用样式放全局主题或设计系统。
- 业务样式跟随组件或 feature。
- 禁止随意覆盖第三方组件内部类名，除非封装成稳定适配层。

## 可访问性

- 交互元素必须可键盘操作。
- 表单控件必须有关联 label。
- 错误状态不能只依赖颜色表达。
- 弹窗和抽屉必须管理焦点。
